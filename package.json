{
  "name": "frisabor",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "dev": "gulp && gulp watch",
    "build": "gulp clean && gulp libs-scripts && gulp scripts && gulp libs-styles && gulp styles"
  },
  "author": "Eudes Roger",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.0",
    "gulp-clean": "^0.4.0",
    "gulp-concat": "^2.6.1",
    "gulp-cssmin": "^0.2.0",
    "gulp-csso": "^3.0.1",
    "gulp-rename": "^1.4.0",
    "gulp-uglify": "^3.0.1",
    "run-sequence": "^2.2.1"
  }
}
